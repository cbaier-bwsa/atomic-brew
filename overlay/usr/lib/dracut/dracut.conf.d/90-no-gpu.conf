# GPU-Treiber (und damit ihre Firmware, ~140 MB) nicht ins initramfs: udev lädt sie nach
# switch_root aus /usr/lib/modules, nur für die verbaute Karte. Bis dahin läuft Plymouth
# über simpledrm (Firmware-Framebuffer, fest im Kernel).
omit_drivers+=" nouveau amdgpu radeon i915 xe "
